import renderer from 'react-test-renderer';
import BottomWrapper from '../../app/containers/BottomMapWrapper';
import GoogleMap from '../../app/containers/GoogleMap';

jest.mock('react-load-script');

describe('Bottom part of the page', () => {
    describe('#GoogleMap', () => {
        it('should render GoogleMap correctly', () => {
            const tree =
        });
    });
});
